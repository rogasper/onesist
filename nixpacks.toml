[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = ["cd landing && bun install --frozen-lockfile"]

[phases.build]
cmds = ["cd landing && bun run build"]

[start]
cmd = "bunx --bun serve landing/dist -l tcp://0.0.0.0:$PORT --single --no-clipboard"
